"""
Simulador local: corre dos instancias del bot sin red, turno a turno,
hasta game over. Se implementa en la Fase C.

TODO(Fase C):
- Generar tablero inicial (cabezas + comida).
- Loop turno a turno alternando side A/B.
- Guardar el log con el mismo formato que game_<id>.log.
"""